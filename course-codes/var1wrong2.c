#include<stdio.h> 
int main( void ) {
  int a, b, c;   /* define variables first:
		    a, b and c are of type int */
  a=10           /* assign values */
  b=20;
  c = a + b;     /* do computation */
  printf("The sum of %d and %d is %d.\n",a,b,c);
  return 0;
}
